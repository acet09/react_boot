import React from 'react';
// react-bootstrap의 버튼 컴포넌트
import { Button} from 'react-bootstrap'

const ButtonComponent = () => {
  return (
    <Button variant='primary'>확인</Button> //라이브러리 사양에 따라 속성을 사용
  )
}

export default ButtonComponent